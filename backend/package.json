{
  "name": "hypr-commplan-backend",
  "version": "1.0.0",
  "description": "HYPR Commplan — backend de cálculo de bônus dos CSs",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "setup-schema": "node scripts/setup-schema.js",
    "functions-framework": "functions-framework --target=commplan --port=8080"
  },
  "dependencies": {
    "@google-cloud/bigquery": "^7.9.1",
    "@google-cloud/functions-framework": "^3.4.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "exceljs": "^4.4.0",
    "express": "^4.21.1",
    "googleapis": "^140.0.0",
    "nodemailer": "^6.9.15"
  },
  "engines": {
    "node": ">=20"
  }
}
